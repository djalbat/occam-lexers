'use strict';

var util = require('../util');

class CommentTokens {
  static pass(tokensOrRemainingContent, line, context) {
    var inComment = false;  ///

    return inComment;
  }
}

module.exports = CommentTokens;
